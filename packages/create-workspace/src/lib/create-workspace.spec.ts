import { createWorkspace } from './create-workspace'

describe('create workspace', () => {
  it('is defined', () => {
    expect(createWorkspace).toBeDefined()
  })

  it.todo('execute a synchrounous command and return the output')
  it.todo('execute a synchrounous command and error when it fails')
})
